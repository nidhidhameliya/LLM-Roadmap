# Mini-Project: Persistent Vector Store
**Brief:** Build a simple CLI tool to manage a persistent vector database.
**Requirements:** Use Qdrant or ChromaDB. Implement commands to `add <text>`, `search <query>`, and `delete <id>`. Store the DB locally on disk.
