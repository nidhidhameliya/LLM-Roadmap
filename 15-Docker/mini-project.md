# Mini-Project: Containerized LLM API
**Brief:** Dockerize the FastAPI app from the previous section.
**Requirements:** Write a `Dockerfile` that uses `python:3.11-slim`, installs requirements, and runs `uvicorn`. Ensure the API is accessible on port 8000 of your host machine.
