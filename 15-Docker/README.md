# Docker

## Core Concepts
- **Containers:** Isolated environments containing your app, Python, and dependencies. Solves the "it works on my machine" problem.
- **Images:** Read-only templates used to build containers (defined by a Dockerfile).
- **Docker Compose:** Tool for defining and running multi-container applications (e.g., FastAPI app + Vector Database).
