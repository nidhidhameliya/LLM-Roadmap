# Fine-Tuning Exercises
1. Format a dataset into JSONL with `{"prompt": "...", "completion": "..."}` format.
2. Load a base model in 4-bit precision using `bitsandbytes`.
3. Configure a `LoraConfig` object in the `peft` library.
