# Mini-Project: QLoRA Fine-Tuning
**Brief:** Fine-tune a small model (e.g., TinyLlama or Phi-2) on a custom dataset.
**Requirements:** Use `trl` and `SFTTrainer` to train a LoRA adapter on a small instruction dataset (e.g., alpaca format) for 1 epoch. Save the adapter weights.
