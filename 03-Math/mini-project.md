# Mini-Project: KV Cache Calculator CLI
**Brief:** Build a CLI tool for ML engineers estimating deployment costs.
**Requirements:** Take model parameters (layers, heads, dim), precision (fp16, fp8, int4), and context length as arguments. Output the memory required for the model weights AND the KV cache, recommending the required GPU (e.g., A100 vs H100).
