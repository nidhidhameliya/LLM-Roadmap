# Tokenization Exercises
1. Use `tiktoken` to encode a string containing emojis, English, and Korean. Compare the token counts.
2. Explore token boundaries: Tokenize " Hello", "Hello", and " hello" to see how whitespace affects token IDs.
