# Mini-Project: Token Cost Estimator
**Brief:** Build a robust API cost estimator handling multimodal tokens.
**Requirements:** Use `tiktoken` to accurately count text tokens. Implement OpenAI's vision token pricing logic (calculating image tiles based on resolution) to estimate the exact cost of a multimodal GPT-4o API call.
