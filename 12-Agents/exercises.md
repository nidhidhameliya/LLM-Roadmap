# Agents Exercises
1. Define a JSON schema for a tool that gets the current weather in a given city.
2. Implement a simple "while loop" in Python that takes an LLM's tool-call request, executes a local python function, and feeds the result back to the LLM.
3. Use LangChain's `@tool` decorator to create a custom calculator tool.
