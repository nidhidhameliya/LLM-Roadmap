# Mini-Project: RAG API Wrapper
**Brief:** Wrap your minimal RAG script (from folder 10) in a FastAPI web server.
**Requirements:** Provide a `POST /query` endpoint. Handle errors gracefully (e.g., return 500 if the LLM API fails). Include an endpoint to upload a new text document to the vector store.
