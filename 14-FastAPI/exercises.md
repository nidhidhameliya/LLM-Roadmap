# FastAPI Exercises
1. Create a `GET /health` endpoint that returns `{"status": "ok"}`.
2. Define a Pydantic `BaseModel` for a chat request containing `user_id` and `message`.
3. Create a `POST /chat` endpoint that accepts the model and simulates an async delay before returning a response.
