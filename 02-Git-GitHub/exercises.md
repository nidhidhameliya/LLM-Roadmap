# Git & DVC Exercises
1. Initialize a Git repo and a DVC pipeline (`dvc init`).
2. Add a 100MB dummy dataset to DVC and push it to a local remote (`dvc add dataset.csv`, `dvc push`).
3. Set up a `.github/workflows/evals.yml` that runs a mock evaluation script on every push to `main`.
