# Mini-Project: Deploy API to local cluster
**Brief:** Deploy your Dockerized FastAPI app to a local K8s cluster.
**Requirements:** Create a `deployment.yaml` and a `service.yaml`. Apply them, and use port-forwarding to access the API from your browser.
