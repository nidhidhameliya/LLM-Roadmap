# Mini-Project: Minimal RAG Script
**Brief:** Build a simple RAG pipeline over a text document.
**Requirements:** Use LangChain to load a local `.txt` file, chunk it, embed it with an open-source model (e.g., `all-MiniLM-L6-v2`), store in ChromaDB, and query it using the OpenAI API.
